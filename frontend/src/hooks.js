// Re-export react and react-router-dom hooks from a single place
import { useState, useEffect, useCallback, useMemo, useRef } from 'react';
import { Routes, Route, Navigate, useNavigate, useLocation, useParams, Link } from 'react-router-dom';

export {
  useState,
  useEffect,
  useCallback,
  useMemo,
  useRef,
  Routes,
  Route,
  Navigate,
  useNavigate,
  useLocation,
  useParams,
  Link,
};
